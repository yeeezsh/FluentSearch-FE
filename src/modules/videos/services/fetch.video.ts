import { videoFile } from '../mocks/videoFile';
import { VideoFileType } from '../models/types';

export const fetchVideo = (): VideoFileType => {
  //TODO: Change to fetch video file and url
  return videoFile;
};
