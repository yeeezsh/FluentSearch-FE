export type _modelCardProps = {
  cardName: string;
  model: string;
  largeNumber: number;
};

export type _numberCardProps = {
  cardName: string;
  largeNumber: number;
  todayNumber: number;
};

export type _progressCardProps = {
  cardName: string;
  progress: number;
  doneNumber: number;
  totalNumber: number;
};
