query GetRecentFiles($limit: Int, $skip: Int) {
  GetRecentFiles(limit: $limit, skip: $skip) {
    result {
      date
      files {
        createAt
        original_filename
        updateAt
        uri
        uri_thumbnail
      }
    }
  }
}
